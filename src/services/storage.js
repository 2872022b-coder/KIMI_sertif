import { STORAGE_KEYS } from '../utils/constants.js';;const storage = {  getUsers: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.USERS) || '[]'),  setUsers: (users) => localStorage.setItem(STORAGE_KEYS.USERS, JSON.stringify(users)),  getProducts: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.PRODUCTS) || '[]'),  setProducts: (products) => localStorage.setItem(STORAGE_KEYS.PRODUCTS, JSON.stringify(products)),  getTransactions: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.TRANSACTIONS) || '[]'),  setTransactions: (transactions) => localStorage.setItem(STORAGE_KEYS.TRANSACTIONS, JSON.stringify(transactions)),  getCart: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.CART) || '[]'),  setCart: (cart) => localStorage.setItem(STORAGE_KEYS.CART, JSON.stringify(cart)),  getAuth: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.AUTH) || 'null'),  setAuth: (auth) => localStorage.setItem(STORAGE_KEYS.AUTH, JSON.stringify(auth)),  getDepositRequests: () => JSON.parse(localStorage.getItem(STORAGE_KEYS.DEPOSIT_REQUESTS) || '[]'),  setDepositRequests: (requests) => localStorage.setItem(STORAGE_KEYS.DEPOSIT_REQUESTS, JSON.stringify(requests)),  generateUserId: () => {    const users = storage.getUsers();    const nextNum = users.length + 1;    return `SRT${String(nextNum).padStart(5, '0')}`;  },  generateTransactionId: () => {    const transactions = storage.getTransactions();    const nextNum = transactions.length + 1;    return `T${String(nextNum).padStart(6, '0')}`;  },  generateDepositRequestId: () => {    const requests = storage.getDepositRequests();    const nextNum = requests.length + 1;    return `DR${String(nextNum).padStart(6, '0')}`;  },};export default storage;