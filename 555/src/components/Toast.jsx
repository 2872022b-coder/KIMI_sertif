import React, { useEffect } from 'react';;import { X, CheckCircle, AlertCircle } from 'lucide-react';;export function Toast({ message, type = 'success', onClose }) {  useEffect(() => {    const timer = setTimeout(onClose, 3000);    return () => clearTimeout(timer);  }, [onClose]);  const Icon = type === 'success' ? CheckCircle : AlertCircle;  const bg = type === 'success' ? 'bg-green-600' : 'bg-red-600';  return (    <div className={`fixed top-4 right-4 z-50 ${bg} text-white px-4 py-3 rounded-lg shadow-lg flex items-center gap-3 animate-fade-in`}>      <Icon size={20} />      <span className='text-sm font-medium'>{message}</span>      <button onClick={onClose} className='hover:opacity-80'><X size={16} /></button>    </div>  );}